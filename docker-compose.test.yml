version: '3.8'

services:
  test_db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: test_mini_logistics
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_pass
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data  # Хранит данные в RAM для скорости